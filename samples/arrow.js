function add(x, y) {
  return x + y;
}

const add = function (x, y) {
  return x + y;
};

const add = (x, y) => {
  return x + y;
};

const add = (x, y) => x + y;

[1, 2, 3, 4].map((v) => v ** 2); // [1, 4, 9, 16]

// () => ...
// x => ...
// (x, y) => ...
